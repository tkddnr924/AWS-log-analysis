{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Frontend may open a native folder picker only. All file access happens in Rust.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
